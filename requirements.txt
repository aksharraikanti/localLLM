python-dotenv
pytest
requests
beautifulsoup4
bleach
# Model fine-tuning dependencies
torch
transformers
datasets
tokenizers
pyyaml
tqdm
# Hyperparameter tuning dependency
optuna

# Quantization & ONNX dependencies
bitsandbytes
onnxruntime
optimum
# ONNX export dependency
# ONNX export dependency
optimum[exporters]

# FlashAttention (optional fast attention)
# FlashAttention (optional fast attention)
#flash-attn

# Benchmarking dependencies
# Benchmarking dependencies
numpy

# Triton kernel tuning (optional)
#triton

# Evaluation metrics dependencies
evaluate
# Evaluation metrics dependencies
evaluate
rouge-score

# Inference API dependencies
fastapi
uvicorn
pydantic
httpx
